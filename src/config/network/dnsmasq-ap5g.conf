# SoundSpot — AP 5GHz bi-bande (fragment /etc/dnsmasq.d/, installé par ap5g.sh)
# Sous-réseau distinct de l'AP 2,4GHz, routé par le Pi (pas de bridge entre les deux radios).
interface=${IFACE_AP5G}
dhcp-range=${AP5G_DHCP_START},${AP5G_DHCP_END},24h
